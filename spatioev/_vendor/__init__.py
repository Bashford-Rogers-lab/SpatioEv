"""Vendored third-party code.

Modules under this package are copied verbatim (apart from documented,
minimal edits) from their upstream projects, with the upstream licence
retained alongside them. They are vendored to avoid pulling heavy transitive
dependency trees for a small number of algorithmic functions.

Nothing here is part of the SpatioEv public API. Import the wrappers in
``spatioev.tl`` instead.
"""
